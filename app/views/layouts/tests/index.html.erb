<h1>Тесты</h1>
<% if @tests.present? %>
  <table>
    <tr>
      <th align="left">Название</th>
      <th>Категория</th>
      <th>Уровень</th>
      <th>Кол-во вопросов</th>
      <% if user_signed_in? %>
        <th colspan="2">Действия</th>
      <% end %>
    </tr>
    <%= render @tests %>
  </table>
<% else %>
  <p>Пока нет ни одного теста</p>
<% end %>
